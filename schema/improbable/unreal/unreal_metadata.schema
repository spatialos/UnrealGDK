package improbable.unreal;

import "improbable/unreal/core_types.schema";

component UnrealMetadata
{
    id = 100004;
    option<string> static_path = 1;
    option<string> owner_worker_id = 2;
    UnrealObjectRef owning_object = 3; // Represents an ownership constraint. NUF will make sure that 'owning_object' is created before this entity.
    map<string, uint32> subobject_name_to_offset = 4;
}